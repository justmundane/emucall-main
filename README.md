# emucall-main
Calls functions inside another process without injecting. A from-scratch x86-64 interpreter that runs the target's own code in your address space on a virtual CPU, faulting real memory in over ReadProcessMemory as it executes.
